# Sentinel

````
启动控制台：
D:
cd D:\IdeaProjects-MyProjects\spring-cloud-valeera\Sentinel
java -Dserver.port=8990 -Dcsp.sentinel.dashboard.server=localhost:8990 -Dproject.name=sentinel-dashboard -jar sentinel-dashboard-1.7.1.jar

其中 -Dserver.port=8080 用于指定 Sentinel 控制台端口为 8080。

localhost:8990

默认用户名和密码都是 sentinel。
````